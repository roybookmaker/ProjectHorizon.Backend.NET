{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "AccountDB": "Host=localhost;Username=postgres;Password=horizonsql;Database=ProjectHorizonAccount" //BE AWARE : host address can be different when using docker or local. IP address is for docker, localhost is for local
  }
}
